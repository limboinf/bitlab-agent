# @bitlab/core

Shared TypeScript DTOs and utilities used by the Bitlab Desktop, WebUI, CLI, and server packages.

Keep this package stable and dependency-light. Public type changes must be validated against `packages/shared`, `packages/server-core`, and `apps/*`.

```bash
cd packages/core
bun run tsc --noEmit
```
